# Deprecated — use `resolve-drift-ticket.md` instead

See `prompts/resolve-drift-ticket.md`. The v3 storyboard drops the "agent instruments the code" beat — code + registry changes now ship together in every PR (enforced by the `observability-watch` workflow).
